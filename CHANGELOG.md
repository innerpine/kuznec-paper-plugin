# Changelog

## Unreleased

- Added HEX color support in text formatting.
- Added `/kuznec reload`.
- Split configuration into `gui.yml`, `messages.yml`, and `effects.yml`.
- Added safer config reload flow with validation before applying changes.
- Added initial automated test coverage.
- Added CI workflow for build and test verification.
